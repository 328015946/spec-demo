---
description: Trellis 任务启动与 PRD 同步规范（中文）
alwaysApply: true
---

# Trellis 任务执行规范（中文）

当用户明确提出“开始任务/新增功能/修复问题”时，默认按以下流程执行：

1. 自动创建并启动任务：
   - 先执行 `task.py create "<任务名>" --slug <slug>`
   - 再执行 `task.py start <task目录>`
2. 自动初始化任务文档：
   - 在 `.trellis/tasks/<task>/prd.md` 写入初版：目标、范围、验收标准
   - 若无 `## 变更记录`，自动补上
3. 需求变更时必须同步 PRD：
   - `prd.md` 以“当前真相”为准
   - 变更后同步更新 `目标 / 范围 / 验收标准`
   - 删除已过期的验收项，不保留在当前标准里
   - 在 `## 变更记录` 追加一条（日期 + 变化内容）
4. 任务收尾前检查一致性：
   - 实现内容与 `prd.md` 必须一致
   - 先提交代码，再执行任务归档
5. 当用户明确说“归档”时，固定按以下顺序执行：
   - 先检查 git 工作区是否存在未提交改动
   - 若存在未提交改动：先执行 `git add .`，再提交代码
   - 若无改动：直接进入归档步骤
   - 最后执行任务归档

变更记录示例：

- `2026-05-08：移除福利亮点模块，验收标准同步删除该项。`
